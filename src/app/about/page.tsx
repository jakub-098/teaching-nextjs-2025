export default function AboutPage() {
  return <div className="font-sans grid grid-rows-[20px_1fr_20px] items-center justify-items-center min-h-screen p-8 pb-20 gap-16 sm:p-20">
        <main className="flex flex-col gap-[32px] row-start-2 items-center sm:items-start">
          
          <h1 className="text-4xl font-bold text-center sm:text-left m-auto">
            About Page
          </h1>
         
  
          

          
  
          {/* <div className="grid grid-cols-6 gap-12">
   
            {albums.map((album) => (
              <div key={album.id} className="bg-green-500 p-8 rounded-md font-semibold font-mono">
                {album.name}: {new Date(album.release_date).toDateString()}
              </div>
              ))}
   
          </div> */}
   
   
          
        </main>
        </div>
}